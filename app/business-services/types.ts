export interface Service {
  title: string;
  icon: React.ReactElement;
  description: string;
  deliverables: string[];
}

export interface Benefit {
  icon: React.ReactElement;
  title: string;
  description: string;
}
